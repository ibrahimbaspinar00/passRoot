org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Build can continue without google-services.json unless this is set to true.
passroot.firebase.required=false

# Optional signing inputs (prefer key.properties for local and env vars for CI):
# passroot.release.storeFile=/absolute/path/to/passroot-release.jks
# passroot.release.storePassword=...
# passroot.release.keyAlias=passroot
# passroot.release.keyPassword=...
# passroot.release.keystoreBase64=...
# passroot.release.keystoreExt=jks
